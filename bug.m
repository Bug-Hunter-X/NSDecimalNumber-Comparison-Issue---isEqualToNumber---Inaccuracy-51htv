In Objective-C, a subtle bug can occur when dealing with `NSDecimalNumber` and comparing its values using `isEqualToNumber:`.  If you are comparing two `NSDecimalNumber` objects that represent the same numerical value but have different internal scales (number of decimal places), `isEqualToNumber:` might return `NO`. This is because the internal representation is not just the numerical value but also includes the scale. For example, `[NSDecimalNumber decimalNumberWithString:@